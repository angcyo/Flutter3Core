# https://pub.dev/packages/flutter_launcher_icons
# dart run flutter_launcher_icons:generate
# dart run flutter_launcher_icons -f Flutter3Core/flutter_launcher_icons.yaml
# 生成之后, 清除缓存, 重新编译↓
# flutter clean
# flutter pub get
# flutter devices
# flutter run -d macos
# flutter run -d windows
# flutter run -d edge
# flutter run -d chrome
# flutter run -d 1C151FDF60063X
# flutter run -d 00008110-000E78DC0A8B801E
flutter_launcher_icons:
  image_path: "assets/ico/app_icon.png"
  android: true
  ios: true
  remove_alpha_ios: true
  windows:
    generate: true
    icon_size: 256 # min:48, max:256, default: 48
  macos:
    generate: true
#  image_path_android: "assets/ico/app_icon.png"
#  image_path_ios: "assets/ico/app_icon.png"
#  android: true # can specify file name here e.g. "ic_launcher"
#  ios: true # can specify file name here e.g. "My-Launcher-Icon"
#  adaptive_icon_background: "assets/images/christmas-background.png" # only available for Android 8.0 devices and above
#  adaptive_icon_foreground: "assets/images/icon-foreground-432x432.png" # only available for Android 8.0 devices and above
#  adaptive_icon_foreground_inset: 16 # only available for Android 8.0 devices and above
#  adaptive_icon_monochrome: "assets/images/icon-monochrome-432x432.png" # only available for Android 13 devices and above
#  min_sdk_android: 21 # android min sdk min:16, default 21
#  remove_alpha_ios: true # WARNING: Icons with alpha channel are not allowed in the Apple App Store.
#  background_color_ios: "#ffffff"
#  windows:
#    generate: true
#    image_path: "assets/ico/app_icon.png"
#    icon_size: 256 # min:48, max:256, default: 48
#  macos:
#    generate: true
#    image_path: "assets/ico/app_icon.png"